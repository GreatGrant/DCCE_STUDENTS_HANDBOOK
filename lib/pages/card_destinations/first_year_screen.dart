import 'package:flutter/cupertino.dart';

class FirstYear extends StatelessWidget {
  const FirstYear({super.key, required this.title});
  final String title;

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
